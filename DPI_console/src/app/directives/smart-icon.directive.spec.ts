import { SmartIconDirective } from './smart-icon.directive';

describe('SmartIconDirective', () => {
  it('should create an instance', () => {
    const directive = new SmartIconDirective();
    expect(directive).toBeTruthy();
  });
});
