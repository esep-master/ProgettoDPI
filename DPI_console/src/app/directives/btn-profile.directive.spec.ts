import { BtnProfileDirective } from './btn-profile.directive';

describe('BtnProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnProfileDirective();
    expect(directive).toBeTruthy();
  });
});
