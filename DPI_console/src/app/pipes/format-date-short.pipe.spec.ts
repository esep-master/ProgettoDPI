import { FormatDateShortPipe } from './format-date-short.pipe';

describe('FormatDateShortPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateShortPipe();
    expect(pipe).toBeTruthy();
  });
});
