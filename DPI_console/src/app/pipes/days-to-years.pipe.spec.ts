import { DaysToYearsPipe } from './days-to-years.pipe';

describe('DaysToYearsPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysToYearsPipe();
    expect(pipe).toBeTruthy();
  });
});
